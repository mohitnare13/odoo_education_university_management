<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_university_grade_tree" model="ir.ui.view">
        <field name="name">university.grade.list</field>
        <field name="model">university.grade</field>
        <field name="arch" type="xml">
            <list string="Grades">
                <field name="name"/>
                <field name="from_percentage"/>
                <field name="to_percentage"/>
                <field name="grade_point"/>
                <field name="remark"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_university_grade_form" model="ir.ui.view">
        <field name="name">university.grade.form</field>
        <field name="model">university.grade</field>
        <field name="arch" type="xml">
            <form string="Grade">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="active"/>
                    </group>
                    <group >
                        <field name="from_percentage"/>
                        <field name="to_percentage"/>
                    </group>
                    <group>
                        <field name="grade_point"/>
                        <field name="remark"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="grade_action" model="ir.actions.act_window">
        <field name="name">Grade</field>
        <field name="res_model">university.grade</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p>
                Create grading ranges based on percentage to be used
                across the university exams and results.
            </p>
        </field>
    </record>

    <menuitem id="menu_grade"
              name="Grade"
              parent="menu_university_school_configuration"
              action="grade_action"/>
</odoo>
